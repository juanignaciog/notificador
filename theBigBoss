pagina="https://daddyyankee.com/"
respuesta=`curl -s $pagina | grep -E -i '{{palabras clave a buscar separadas por '|' ej: daddy|yankee}}'`
usuarios="{{listado de emails destino separados por ',' sin espacios}}"
hora=`date +%T`
if [ -z "$respuesta" ]
then
 echo "No hubo cambios $hora" >> {{nombre de archivo de log}}.log
# echo "No hubo cambios en la pagina" | mail -s '{{asunto del mail}}' -c usuarios {{email del remitente}} 
else
 echo "hubo cambios en la pagina" >> {{nombre de archivo de log}}.log
 echo "AAH MIRÁ LA PAGINA DE DADDY YANKEE!!" | mail -s '{{asunto del mail}}' -c $usuarios {{email del remitente}}
fi
